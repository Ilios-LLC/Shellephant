needs to be a svelte electron app
needs to spin up something called a window
a window is a docker container made with the cc image
when it's spun up, users can click the window and it opens a bash shell where users can input text into a terminal in the container users should be able to type input and see output in the window just like they would in a terminal app.
use sqlite to save window details.
have a delete button that soft deletes and kills the container
